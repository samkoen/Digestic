# Copier vers .env.test (non versionné) et adapter les valeurs.
# Utilisé par : tests d'intégration pytest, et Alembic si DIGESTIC_ENV=test.

DATABASE_URL=postgresql://UTILISATEUR:MOT_DE_PASSE@localhost:5432/digestic_test

# Optionnel : même secret que le dev si tu lances l’app dans les tests avec sessions.
# SECRET_KEY=...
