/**
 * Liste paginée (Pharmacies, Factures, Bons de livraison).
 * Hauteur maximale pour scroll interne : la barre horizontale du tableau
 * reste au bas de la zone visible, pas après toutes les lignes.
 *
 * Ajuster selon disposition AppBar, titres, barres de filtres.
 */
export const LIST_TABLE_SCROLL_MAX_HEIGHT = 'calc(100vh - 232px)'
